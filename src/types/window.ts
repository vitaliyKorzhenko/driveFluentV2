export interface IWindow {
    description: string;
    label: string;
    nodename: string;
}

export interface IWindowItem {
    description: string;
    label: string;
    nodename: string;
    multi: boolean;
    required: boolean;
}